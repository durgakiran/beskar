import type { Editor } from '@tiptap/core';

/** @deprecated Code blocks now provide their own accessible edit/view controls. */
export function CodeBlockFloatingMenu(_props: { editor: Editor }) { return null; }
